<tr>
  <td><%= link_to director.name, :action => 'show', :id => director %></td>
  <td><%= link_to 'Editar', :action => 'edit', :id => director %></td>
  <td>
    <%= button_to 'Eliminar', { :action => 'destroy', :id => director },
        data: { confirm: "¿Está seguro de que quiere eliminar el director #{director.name}?" } %>
  </td>
</tr>
